{
  "name": "butuz.com",
  "private": true,
  "version": "1.0.0",
  "description": "Butuz.com — социальная сеть + казино (React + Node.js + SQLite)",
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "dev": "concurrently -k -n \"server,client\" -c \"blue,green\" \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "start:server": "cd server && npm start",
    "seed": "cd server && npm run seed",
    "build": "cd client && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
